import { memo } from "react"

export default memo(({flightData}: {flightData: OnlineFlight}) => {
    return (
        <div>
            <div>
                <div>
                    <div></div>
                </div>
            </div>
        </div>
    )
})